import React, { Component } from 'react'
import { Text, View } from 'react-native'
import { AccountScreenBackground, BackgroundWrapper } from '../components/account.style'

export const LoginScreen = () => {
    return (
        <AccountScreenBackground />

    )
} 